@model Room
@{
    ViewData["Title"] = "Delete room";
}
<div>
    <table class="table">
        <thead>
            <tr>
                <th>Room</th>
                <th>Price</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>@Model.RoomNumber</td>
                <td>@Model.Price</td>
            </tr>
        </tbody>
    </table>
</div>


<div class="col-md-4 offset-md-4 mt-5">
    <div class="card" style="border-color: red">
        <div class="card-body">
            <form asp-action="DeleteRoomConfirmed" method="post" class="delete-form">
                <input asp-for="Id" />
                <p>Are you sure you want to delete?</p>
                <button type="submit" class="btn btn-danger">Yes</button>
                <a asp-controller="Home" asp-action="RoomsList" class="btn btn-secondary">No</a>
            </form>
        </div>
    </div>
</div>